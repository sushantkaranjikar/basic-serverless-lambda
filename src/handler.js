exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Welcome to TechnoCirrus 1!!!'),
    };
    return response;
};
